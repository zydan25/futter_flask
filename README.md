# futter_flask

Server-Driven Flutter Runtime backend.

The production migration source is being prepared from `zydan25/flask/server-runtime-v2` and will replace the current runtime only after CI and end-to-end validation pass.
